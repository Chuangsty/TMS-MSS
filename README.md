# TMS-MSS

# On new start-up: run db script once

npm run db:setup

# Viewing db in my sql:

mysql -u root -p
USE tms_mss
SHow tables

# Running the program of back and front end respectively:

npm run dev: runs the app
